import { createStore } from 'vuex'

const shoppingCart = {
  namespaced: true,
  actions: {},
  mutations: {},
  state: {},
  getters: {}
}

export default createStore({
  modules: {
    shoppingCart
  }
})
